AWSTemplateFormatVersion: '2010-09-09'
Description: 'DAT409 - Code Editor Infrastructure Setup'

Parameters:
  TemplateName:
    Type: String
    Description: Template name prefix for resources
    
  CodeEditorUser:
    Type: String
    Default: participant
    Description: Username for Code Editor
    
  InstanceName:
    Type: String
    Description: Instance name
    
  InstanceVolumeSize:
    Type: Number
    Default: 50
    Description: Volume size in GB
    
  InstanceType:
    Type: String
    Default: t4g.large
    Description: EC2 instance type
    
  InstanceOperatingSystem:
    Type: String
    Default: AmazonLinux-2023
    Description: Operating system
    
  HomeFolder:
    Type: String
    Default: /workshop
    Description: Working directory
    
  RepoUrl:
    Type: String
    Default: https://github.com/aws-samples/sample-dat409-hybrid-search-aurora-mcp.git
    Description: GitHub repository URL
    
  BootstrapScriptUrl:
    Type: String
    Default: https://raw.githubusercontent.com/aws-samples/sample-dat409-hybrid-search-aurora-mcp/main/scripts/bootstrap-code-editor-unified.sh
    Description: URL to the unified bootstrap script
    
  VPCId:
    Type: String
    Description: VPC ID
    
  SubnetId:
    Type: String
    Description: Subnet ID
    
  VpcCIDR:
    Type: String
    Description: VPC CIDR
    
  DBSecretArn:
    Type: String
    Description: Database secret ARN
    
  DBClusterEndpoint:
    Type: String
    Description: Database endpoint
    
  DBClusterArn:
    Type: String
    Description: Database cluster ARN for Data API
    
  DBName:
    Type: String
    Default: workshop_db
    Description: Database name
  
  PythonVersion:
    Type: String
    Default: "3.13"
    Description: Python version

  PostgreSQLVersion:
    Type: String
    Default: "17.5"
    Description: PostgreSQL version
  
  AssetsBucketName:
    Type: String
    Default: ''
    Description: S3 bucket containing workshop assets
  
  AssetsBucketPrefix:
    Type: String
    Default: ''
    Description: S3 prefix for workshop assets

Conditions:
  IsGraviton: !Or
    - !Equals [!Select [0, !Split ['.', !Ref InstanceType]], 't4g']
    - !Equals [!Select [0, !Split ['.', !Ref InstanceType]], 'c6g']
    - !Equals [!Select [0, !Split ['.', !Ref InstanceType]], 'm6g']
    - !Equals [!Select [0, !Split ['.', !Ref InstanceType]], 'r6g']

Mappings:
  ArmImage:
    AmazonLinux-2023:
      ImageId: '{{resolve:ssm:/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-arm64}}'
  AmdImage:
    AmazonLinux-2023:
      ImageId: '{{resolve:ssm:/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64}}'
  AWSRegionsPrefixListID:
    us-east-1:
      PrefixList: pl-3b927c52
    us-east-2:
      PrefixList: pl-b6a144df
    us-west-1:
      PrefixList: pl-4ea04527
    us-west-2:
      PrefixList: pl-82a045eb
    eu-west-1:
      PrefixList: pl-4fa04526
    eu-central-1:
      PrefixList: pl-a3a144ca
    ap-southeast-1:
      PrefixList: pl-31a34658
    ap-southeast-2:
      PrefixList: pl-b8a742d1
    ap-northeast-1:
      PrefixList: pl-58a04531

Resources:
  # Code Editor Secret
  CodeEditorSecret:
    Type: AWS::SecretsManager::Secret
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      Name: !Sub
        - ${InstanceName}-${RandomGUID}
        - RandomGUID: !Select [0, !Split ['-', !Select [2, !Split ['/', !Ref AWS::StackId ]]]]
      Description: Code Editor user credentials
      GenerateSecretString:
        PasswordLength: 16
        SecretStringTemplate: !Sub '{"username":"${CodeEditorUser}"}'
        GenerateStringKey: 'password'
        ExcludePunctuation: true

  # Lambda for extracting secret
  SecretPlaintextLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SecretAccess
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action: secretsmanager:GetSecretValue
                Resource: !Ref CodeEditorSecret

  SecretPlaintextLambda:
    Type: AWS::Lambda::Function
    Properties:
      Handler: index.lambda_handler
      Runtime: python3.13
      Role: !GetAtt SecretPlaintextLambdaRole.Arn
      Code:
        ZipFile: |
          import boto3
          import json
          import cfnresponse
          def lambda_handler(event, context):
              try:
                  if event['RequestType'] == 'Delete':
                      cfnresponse.send(event, context, cfnresponse.SUCCESS, {})
                      return
                  client = boto3.client('secretsmanager')
                  response = client.get_secret_value(SecretId=event['ResourceProperties']['SecretArn'])
                  secret = json.loads(response['SecretString'])
                  cfnresponse.send(event, context, cfnresponse.SUCCESS, secret, noEcho=True)
              except Exception as e:
                  cfnresponse.send(event, context, cfnresponse.FAILED, {}, str(e))

  SecretPlaintext:
    Type: Custom::SecretPlaintextLambda
    Properties:
      ServiceToken: !GetAtt SecretPlaintextLambda.Arn
      SecretArn: !Ref CodeEditorSecret

  # Security Group
  SecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub '${TemplateName} Code Editor security group'
      VpcId: !Ref VPCId
      SecurityGroupIngress:
        - Description: CloudFront access
          IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          SourcePrefixListId: !FindInMap [AWSRegionsPrefixListID, !Ref 'AWS::Region', PrefixList]
        - Description: VPC access for HTTP
          IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: !Ref VpcCIDR
      SecurityGroupEgress:
        - Description: Allow all outbound
          IpProtocol: -1
          CidrIp: 0.0.0.0/0

  # IAM Role
  CodeEditorRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: [ec2.amazonaws.com, ssm.amazonaws.com]
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
        - arn:aws:iam::aws:policy/AmazonBedrockFullAccess
        - arn:aws:iam::aws:policy/AmazonQDeveloperAccess
      Policies:
        - PolicyName: WorkshopAccess
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource:
                  - !Ref CodeEditorSecret
                  - !Ref DBSecretArn
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::${AssetsBucketName}/*'
                  - !Sub 'arn:aws:s3:::${AssetsBucketName}'
                  - !Sub 'arn:aws:s3:::ws-assets-prod-iad-r-${AWS::Region}-*/*'
                  - !Sub 'arn:aws:s3:::ws-assets-prod-iad-r-${AWS::Region}-*'
              - Effect: Allow
                Action:
                  - rds:DescribeDBClusters
                  - rds:DescribeDBInstances
                Resource: '*'
              - Effect: Allow
                Action:
                  - rds-data:BeginTransaction
                  - rds-data:CommitTransaction
                  - rds-data:ExecuteStatement
                  - rds-data:RollbackTransaction
                  - rds-data:BatchExecuteStatement
                Resource: '*'

  CodeEditorInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles:
        - !Ref CodeEditorRole

  # CloudWatch Log Groups
  BootstrapLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/ec2/${TemplateName}/bootstrap'
      RetentionInDays: 7

  SSMDocumentLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/ssm/${TemplateName}-bootstrap'
      RetentionInDays: 7

  # SSM Document with Automated Database Setup
  CodeEditorSSMDoc:
    Type: AWS::SSM::Document
    Properties:
      DocumentType: Command
      Content:
        schemaVersion: '2.2'
        description: DAT409 Code Editor Infrastructure Bootstrap
        parameters:
          CodeEditorPassword:
            type: String
            default: 'defaultPassword'
          BootstrapScriptUrl:
            type: String
            default: !Ref BootstrapScriptUrl
          RepoUrl:
            type: String
            default: !Ref RepoUrl
          HomeFolder:
            type: String
            default: !Ref HomeFolder
          DBSecretArn:
            type: String
            default: !Ref DBSecretArn
          DBClusterEndpoint:
            type: String
            default: !Ref DBClusterEndpoint
          DBClusterArn:
            type: String
            default: !Ref DBClusterArn
          DBName:
            type: String
            default: !Ref DBName
          AssetsBucket:
            type: String
            default: ''
          AssetsBucketPrefix:
            type: String
            default: ''
        mainSteps:
          - name: InstallCloudWatchAgent
            action: aws:configurePackage
            inputs:
              name: AmazonCloudWatchAgent
              action: Install

          - name: SetupEnvironment
            action: aws:runShellScript
            inputs:
              timeoutSeconds: 300
              runCommand:
                - '#!/bin/bash'
                - 'set -euo pipefail'
                - !Sub |
                  echo "Setting up environment variables..."
                  dnf update -y
                  export AWS_REGION="${AWS::Region}"
                  export AWS_DEFAULT_REGION="${AWS::Region}"
                  export AWS_ACCOUNTID="${AWS::AccountId}"
                  echo "Environment setup completed"

          - name: RunGitHubBootstrap
            action: aws:runShellScript
            inputs:
              timeoutSeconds: 1200
              runCommand:
                - '#!/bin/bash'
                - 'set -euo pipefail'
                - !Sub |
                  echo "=== DAT409 Unified Bootstrap (Code Editor + Database) ==="
                  echo "Timestamp: $$(date)"
                  
                  # Export all parameters as environment variables
                  export DB_SECRET_ARN="{{ DBSecretArn }}"
                  export DB_CLUSTER_ENDPOINT="{{ DBClusterEndpoint }}"
                  export DB_CLUSTER_ARN="{{ DBClusterArn }}"
                  export DB_NAME="{{ DBName }}"
                  export AWS_REGION="${AWS::Region}"
                  export ASSETS_BUCKET="{{ AssetsBucket }}"
                  export ASSETS_PREFIX="{{ AssetsBucketPrefix }}"
                  
                  curl -fsSL "{{ BootstrapScriptUrl }}" -o /tmp/bootstrap-unified.sh
                  
                  if [ ! -f /tmp/bootstrap-unified.sh ]; then
                    echo "ERROR: Failed to download unified bootstrap script"
                    exit 1
                  fi
                  
                  chmod +x /tmp/bootstrap-unified.sh
                  
                  echo "Executing unified bootstrap (Code Editor + Database)..."
                  /tmp/bootstrap-unified.sh "{{ CodeEditorPassword }}"
                  
                  if [ $$? -eq 0 ]; then
                    echo "✅ Unified bootstrap completed"
                  else
                    echo "❌ Bootstrap failed"
                    exit 1
                  fi
                  
                  rm -f /tmp/bootstrap-unified.sh

          - name: CloneWorkshopRepository
            action: aws:runShellScript
            inputs:
              timeoutSeconds: 600
              runCommand:
                - '#!/bin/bash'
                - |
                  if [ -n "{{ RepoUrl }}" ]; then
                    echo "Cloning workshop repository..."
                    sudo -u participant git clone "{{ RepoUrl }}" /tmp/workshop_repo
                    
                    if [ -d /tmp/workshop_repo ]; then
                      sudo -u participant cp -r /tmp/workshop_repo/* "{{ HomeFolder }}/" || true
                      sudo -u participant cp -r /tmp/workshop_repo/.[!.]* "{{ HomeFolder }}/" 2>/dev/null || true
                      rm -rf /tmp/workshop_repo
                      
                      # Make setup scripts executable
                      chmod +x "{{ HomeFolder }}/scripts/setup-database.sh" 2>/dev/null || true
                      chmod +x "{{ HomeFolder }}/scripts/verify-setup.sh" 2>/dev/null || true
                      
                      # Disable git operations for participants
                      if [ -d "{{ HomeFolder }}/.git" ]; then
                        echo "Disabling git operations..."
                        chmod -R 000 "{{ HomeFolder }}/.git" 2>/dev/null || true
                        rm -rf "{{ HomeFolder }}/.git/hooks" 2>/dev/null || true
                      fi
                      
                      chown -R participant:participant "{{ HomeFolder }}"
                      
                      echo "✅ Repository cloned successfully"
                    else
                      echo "WARNING: Repository clone failed"
                    fi
                  fi

          - name: InstallLabRequirements
            action: aws:runShellScript
            inputs:
              timeoutSeconds: 600
              runCommand:
                - '#!/bin/bash'
                - |
                  echo "Installing Lab 1 requirements..."
                  if [ -f "{{ HomeFolder }}/lab1-hybrid-search/requirements.txt" ]; then
                    sudo -u participant python3.13 -m pip install --user -r "{{ HomeFolder }}/lab1-hybrid-search/requirements.txt"
                    echo "✅ Lab 1 requirements installed"
                  fi
                  
                  echo "Installing Lab 2 requirements..."
                  if [ -f "{{ HomeFolder }}/lab2-mcp-agent/requirements.txt" ]; then
                    sudo -u participant python3.13 -m pip install --user -r "{{ HomeFolder }}/lab2-mcp-agent/requirements.txt"
                    echo "✅ Lab 2 requirements installed"
                  fi

          - name: FinalValidation
            action: aws:runShellScript
            inputs:
              timeoutSeconds: 300
              runCommand:
                - '#!/bin/bash'
                - |
                  echo "=== FINAL VALIDATION ==="
                  echo "Timestamp: $(date)"
                  
                  sleep 30
                  
                  # Service status
                  NGINX_STATUS=$(systemctl is-active nginx)
                  CODE_EDITOR_STATUS=$(systemctl is-active code-editor@participant)
                  
                  echo "Service Status:"
                  echo "  Nginx: $NGINX_STATUS"
                  echo "  Code Editor: $CODE_EDITOR_STATUS"
                  
                  # Port check
                  echo ""
                  echo "Listening Ports:"
                  ss -tlpn | grep -E ":(80|8080)" || echo "  WARNING: Expected ports not found"
                  
                  # Connectivity test
                  CODE_RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8080/ || echo "000")
                  NGINX_RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1/ || echo "000")
                  
                  echo ""
                  echo "Connectivity Tests:"
                  echo "  Code Editor (8080): $CODE_RESPONSE"
                  echo "  Nginx (80): $NGINX_RESPONSE"
                  
                  # Repository check
                  echo ""
                  echo "Workshop Files:"
                  if [ -f "/workshop/scripts/setup-database.sh" ]; then
                    echo "  ✅ setup-database.sh present"
                  else
                    echo "  ❌ setup-database.sh missing"
                  fi
                  
                  # Overall validation - be more lenient
                  echo ""
                  VALIDATION_PASSED=true
                  
                  if [[ "$NGINX_STATUS" != "active" ]]; then
                    echo "  ❌ Nginx not active"
                    VALIDATION_PASSED=false
                  fi
                  
                  if [[ "$CODE_EDITOR_STATUS" != "active" ]]; then
                    echo "  ❌ Code Editor not active"
                    VALIDATION_PASSED=false
                  fi
                  
                  # Accept any HTTP response (including redirects, auth challenges)
                  if [[ ! "$CODE_RESPONSE" =~ ^[2-5][0-9][0-9]$ ]]; then
                    echo "  ⚠️  Code Editor HTTP response unexpected: $CODE_RESPONSE"
                  fi
                  
                  if [[ ! "$NGINX_RESPONSE" =~ ^[2-5][0-9][0-9]$ ]]; then
                    echo "  ⚠️  Nginx HTTP response unexpected: $NGINX_RESPONSE"
                  fi
                  
                  if [[ "$VALIDATION_PASSED" == "true" ]]; then
                    echo ""
                    echo "✅ UNIFIED SETUP COMPLETE (Code Editor + Database)"
                    echo ""
                    echo "📘 Next Steps:"
                    echo "  1. Access Code Editor via CloudFront URL"
                    echo "  2. Database is pre-loaded and ready to use"
                    echo "  3. Open lab1-hybrid-search/notebook/dat409-hybrid-search-notebook.ipynb"
                    exit 0
                  else
                    echo ""
                    echo "❌ VALIDATION FAILED - Check service status above"
                    exit 1
                  fi

  # Lambda for running SSM document
  SSMDocLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SSMDocOnEC2
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - ssm:SendCommand
                Resource:
                  - !Sub arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:document/${CodeEditorSSMDoc}
                  - !Sub arn:${AWS::Partition}:ec2:${AWS::Region}:${AWS::AccountId}:instance/${CodeEditorInstance}
              - Effect: Allow
                Action:
                  - ssm:ListCommandInvocations
                  - ssm:GetCommandInvocation
                Resource: '*'

  RunSSMDocLambda:
    Type: AWS::Lambda::Function
    Properties:
      Handler: index.lambda_handler
      Runtime: python3.13
      MemorySize: 128
      Timeout: 900
      Environment:
        Variables:
          RetrySleep: 2900
          AbortTimeRemaining: 3200
      Architectures:
        - arm64
      Role: !GetAtt SSMDocLambdaRole.Arn
      Code:
        ZipFile: |
          import boto3
          import cfnresponse
          import logging
          import time
          import os

          logger = logging.getLogger()
          logger.setLevel(logging.INFO)

          def lambda_handler(event, context):
              if event['RequestType'] != 'Create':
                  cfnresponse.send(event, context, cfnresponse.SUCCESS, responseData={}, reason='No action to take')
              else:
                  sleep_ms = int(os.environ.get('RetrySleep'))
                  abort_time_remaining_ms = int(os.environ.get('AbortTimeRemaining'))
                  resource_properties = event['ResourceProperties']
                  instance_id = resource_properties['InstanceId']
                  document_name = resource_properties['DocumentName']
                  cloudwatch_log_group_name = resource_properties['CloudWatchLogGroupName']

                  logger.info(f'Running SSM Document {document_name} on EC2 instance {instance_id}')

                  del resource_properties['ServiceToken']
                  if 'ServiceTimeout' in resource_properties:
                      del resource_properties['ServiceTimeout']
                  del resource_properties['InstanceId']
                  del resource_properties['DocumentName']
                  del resource_properties['CloudWatchLogGroupName']
                  if 'PhysicalResourceId' in resource_properties:
                      del resource_properties['PhysicalResourceId']

                  parameters = {}
                  for key, value in resource_properties.items():
                      parameters[key] = [value]

                  retry = True
                  attempt_no = 0
                  time_remaining_ms = context.get_remaining_time_in_millis()
                  ssm = boto3.client('ssm')

                  while retry:
                      attempt_no += 1
                      logger.info(f'Attempt: {attempt_no}. Time Remaining: {time_remaining_ms/1000}s')
                      try:
                          response = ssm.send_command(
                              InstanceIds = [instance_id],
                              DocumentName = document_name,
                              CloudWatchOutputConfig = {'CloudWatchLogGroupName': cloudwatch_log_group_name, 'CloudWatchOutputEnabled': True},
                              Parameters = parameters
                          )
                          command_id = response['Command']['CommandId']
                          responseData = {'CommandId': command_id}
                          cfnresponse.send(event, context, cfnresponse.SUCCESS, responseData, reason='OK')
                          retry = False

                      except ssm.exceptions.InvalidInstanceId:
                          time_remaining_ms = context.get_remaining_time_in_millis()
                          if time_remaining_ms > abort_time_remaining_ms:
                              logger.info(f'Instance {instance_id} not ready. Sleeping: {sleep_ms/1000}s')
                              time.sleep(sleep_ms/1000)
                              retry = True
                          else:
                              logger.info(f'Instance {instance_id} not ready, timed out.')
                              cfnresponse.send(event, context, cfnresponse.FAILED, responseData={}, reason='Timed out')
                              retry = False

                      except Exception as e:
                          logger.error(e, exc_info=True)
                          cfnresponse.send(event, context, cfnresponse.FAILED, responseData={}, reason=str(e))
                          retry = False

  # EC2 Instance
  CodeEditorInstance:
    Type: AWS::EC2::Instance
    Properties:
      ImageId: !If
        - IsGraviton
        - !FindInMap [ArmImage, !Ref InstanceOperatingSystem, ImageId]
        - !FindInMap [AmdImage, !Ref InstanceOperatingSystem, ImageId]
      InstanceType: !Ref InstanceType
      BlockDeviceMappings:
        - DeviceName: /dev/xvda
          Ebs:
            VolumeSize: !Ref InstanceVolumeSize
            VolumeType: gp3
            DeleteOnTermination: true
            Encrypted: true
      SubnetId: !Ref SubnetId
      SecurityGroupIds:
        - !Ref SecurityGroup
      IamInstanceProfile: !Ref CodeEditorInstanceProfile
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          mkdir -p ${HomeFolder}
      Tags:
        - Key: Name
          Value: !Sub '${TemplateName}-code-editor-instance'

  # Run SSM Document
  RunCodeEditorSSMDoc:
    Type: Custom::RunSSMDocLambda
    Properties:
      ServiceToken: !GetAtt RunSSMDocLambda.Arn
      ServiceTimeout: 905
      InstanceId: !Ref CodeEditorInstance
      DocumentName: !Ref CodeEditorSSMDoc
      CloudWatchLogGroupName: !Ref SSMDocumentLogGroup
      CodeEditorPassword: !GetAtt SecretPlaintext.password
      BootstrapScriptUrl: !Ref BootstrapScriptUrl
      RepoUrl: !Ref RepoUrl
      HomeFolder: !Ref HomeFolder
      DBSecretArn: !Ref DBSecretArn
      DBClusterEndpoint: !Ref DBClusterEndpoint
      DBClusterArn: !Ref DBClusterArn
      DBName: !Ref DBName
      AssetsBucket: !Ref AssetsBucketName
      AssetsBucketPrefix: !Ref AssetsBucketPrefix

  # Cache Policy
  CodeEditorCachePolicy:
    Type: AWS::CloudFront::CachePolicy
    Properties:
      CachePolicyConfig:
        DefaultTTL: 86400
        MaxTTL: 31536000
        MinTTL: 1
        Name: !Sub
          - ${InstanceName}-${RandomGUID}
          - RandomGUID: !Select [0, !Split ['-', !Select [2, !Split ['/', !Ref AWS::StackId ]]]]
        ParametersInCacheKeyAndForwardedToOrigin:
          CookiesConfig:
            CookieBehavior: all
          EnableAcceptEncodingGzip: False
          HeadersConfig:
            HeaderBehavior: whitelist
            Headers:
              - Accept-Charset
              - Authorization
              - Origin
              - Accept
              - Referer
              - Host
              - Accept-Language
              - Accept-Encoding
              - Accept-Datetime
          QueryStringsConfig:
            QueryStringBehavior: all

  # CloudFront Distribution
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    DependsOn: RunCodeEditorSSMDoc
    Properties:
      DistributionConfig:
        Enabled: true
        HttpVersion: http2and3
        DefaultCacheBehavior:
          AllowedMethods: [GET, HEAD, OPTIONS, PUT, PATCH, POST, DELETE]
          CachePolicyId: !Ref CodeEditorCachePolicy
          OriginRequestPolicyId: 216adef6-5c7f-47e4-b989-5492eafa07d3
          TargetOriginId: !Sub 'origin-${AWS::StackName}'
          ViewerProtocolPolicy: allow-all
        Origins:
          - DomainName: !GetAtt CodeEditorInstance.PublicDnsName
            Id: !Sub 'origin-${AWS::StackName}'
            CustomOriginConfig:
              OriginProtocolPolicy: http-only

Outputs:
  URL:
    Description: Code Editor URL
    Value: !Sub 'https://${CloudFrontDistribution.DomainName}/?folder=${HomeFolder}&tkn=${SecretPlaintext.password}'
    
  Username:
    Description: Username
    Value: !GetAtt SecretPlaintext.username
    
  Password:
    Description: Password
    Value: !GetAtt SecretPlaintext.password
    
  InstanceId:
    Description: EC2 Instance ID
    Value: !Ref CodeEditorInstance

  BootstrapLogs:
    Description: CloudWatch Logs
    Value: !Sub 'https://console.aws.amazon.com/cloudwatch/home?region=${AWS::Region}#logsV2:log-groups/log-group/${SSMDocumentLogGroup}'
